
Building the Example

To build the example, you need to build qlalr (it's under src/util/qlalr). A
qmake and make should do just fine. Then you must run qlalr on the plover.g
file, simply 'qlalr plover.g'. Finally, run qmake and make in the example
directory.

